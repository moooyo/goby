# Programs transition admission verification, 2026-09-17

One run on `ssh test-env` passed the complete successor component group: **46 unittest methods, zero failures, skips or retries**. This comprises 36 existing methods and 10 new methods; subtest combinations are not added to the count. The earlier 45 runtime and 71 closer checks were not rerun or added to this result. Exact source and evidence pins are in the [JSON checkpoint](programs-transition-admission-verification-20260917.json).

The two-file proposal removes the operator's three-line unconditional Programs dispatch hold and extends its existing tests. The tests cover both explicit predecessor-recovery modes, fresh-state and retention rejection, CLI routing and interruption, and after-start state or result-write failures through the real recovery method and original CLI failure branch. Filesystem, process, service, capture and HTTP effects remain finite mocks or saved fixtures. The runtime helper, recovery policy, helper pins, budgets and fresh preservation checks are unchanged. Three source files were frozen for this verification.

The unique scope was `/opt/goby-test/programs-transition-admission-component-checks-20260917-3993f552a502`. Original SSH session `12173` exited with code 0. The runner took 14.990 seconds, the dispatcher took 15.422 seconds, and peak unit memory was 104,968,192 bytes. The retained limits were 256 MiB memory, no swap, CPU 25%, TasksMax 32, 60 seconds per group, 240 seconds per unit, a 300-second outer bound and 8 MiB per output file. Fresh admission observed 5,571,960,832 available memory bytes and 45,019,627,520 root-space bytes against the selected 1 GiB / 2 GiB margins.

Source and tool pins remained unchanged. The closure reader recorded 26 PIDs and 24 process groups absent, the component and original SSH-session cgroups empty, and scratch empty. It read back 50 raw streams totaling 36,534 bytes and recorded 65 checked read-descriptor closures; descriptor numbers were reused. Process exit supports closure of the owned processes' descriptors, without a separate process FD census. No OS or file lock was acquired. The owned component unit was started and stopped once.

Independent saved-evidence review matched the 46 unique passing stderr entries to the source's method inventory, including both new after-start failure tests, and checked the source, adapter diffs, raw streams and saved unit/session closure bindings. That review did not rerun tests or perform live checks.

At verification time, main and earlier published operator copies retained their original hold. No actual A/B/main transition or recovery, PostgreSQL or application service action, SQL, HTTP, browser, key-body read, capture or product-loader invocation ran in this scope. The result supports only the named component proposal; formal transition-source publication, fresh entry/recovery admission, client acceptance and the remaining M2-M6 obligations stay separate.
