# First subtitle acceptance on the TV successor

Status: [fresh entry review passed](audited-subtitles-client01-entry.json);
execution pending. This is one
unconsumed `subtitles-01` decision under the [current execution plan](../planning/current-execution-plan.md).
Use the existing subtitle actor, movie and external SRT/VTT fixtures. No account
reset, media preparation, rescan, new client or network change is included.

The baseline is [Episode01's closed owned state](audited-episode-client01-closeout.json):
twenty revoked sessions, eleven plays, five userdata rows, two retained audio
references and no encoding jobs. Preserve all existing rows, including the
counted episode play and its unstarted Prepared history. Episode's failed UI
acceptance does not pass another gate; it also does not prevent this independent
unused actor from entering after exact state and runtime checks.

Select the [verified native-rejection adapter and subtitle readiness correction](native-rejection-observer-verification.json)
with the unchanged admitted product. Fresh entry must bind all35 tables and
five sequences to Episode01's source-after snapshot, verify unused subtitle
actor history, candidate/PostgreSQL/lease/hosting identity and absent new output
and unit names. Freeze that reviewed input before its single execution.

The declared journey logs in through the original UI, opens the exact movie
detail and waits for a unique Play/From Beginning control. Require advancing
video and an early pause preserving the opening cue window. Select English SRT,
observe the opening cue, seek to the forward cue around120 seconds, then select
English VTT and observe the same cue. Restore Off and verify no active displayed
cue, return Back/stop, then logout. Require physical delivery of both subtitle
tracks and the owned media, plus exact playback/authentication/durable evidence.

Native rejection metadata is secondary observation. Keep the existing pageerror
rule. A directly matched Response can support a later focused decision; undefined
remains unattributed. A clean subtitle run does not explain historical movie or
episode errors. Do not add playback attempts merely to reproduce those errors.

The entry snapshot at `2026-09-13T17:30:58.134502Z` matches all35 baseline tables
and all five sequences. The unused actor is `3f1bf6ff4eb186c681a4c579e50b06ba`;
candidate PID486706, PostgreSQL PID363520, lease and hosting are unchanged.
Input `candidate-subtitles-client01-entry-review-01/input.json` under the retained
delivery root has SHA-256
`3b749ff64459675b4b1b27aa6720cd44eaf7d8afd15a894269bc9ea85f496780`;
the review SHA-256 is
`485a4dd96bb5ce9e4565008c445ae67c5d06ba4ed7dd073648ffb98fae779dd7`.
The review issued no business HTTP or service operation.

Use the unchanged 1,200-second controller with 240 seconds reserved for cleanup,
600-second browser with 120 seconds reserved for cleanup, and existing gateway
request/body/idle/concurrency limits. Close all actual owned state before any
further business run. Recorded unstarted preparations must be explained and
retained; failure does not authorize deleting history or automatically retrying.
