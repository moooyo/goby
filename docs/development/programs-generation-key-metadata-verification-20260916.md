# Programs generation-key metadata verification, 2026-09-16

The existing Programs capture now admits exactly one retained inactive-generation master as metadata only. Its four changed source/test files passed one remote 151-check run (44 runtime, 36 successor, 71 closer), with no failures or skips and a passed independent saved-evidence review. Exact source and receipt pins are in the [JSON checkpoint](programs-generation-key-metadata-verification-20260916.json). No new capture, product-loader invocation, transition, or application operation ran.

The authority is the hash-bound Sep13 TV-parent `after.json`, SHA-256 `047e68ffc9fc932146b365430b9ae00720ab6b7708cd52fce623f5c1f0388496`, selected by the unchanged historical epoch. Its registry, cancelled restore operation, recovery slot, inactive database marker and tree metadata agree on generation `60a5025ee4b70e9ac1f43ec611c227e3` and the exact relative path `generation-60a5025ee4b70e9ac1f43ec611c227e3/master.key`. Another generation, unknown key, traversal, missing key, or renamed key is rejected.

Only six historical key identity fields constrain current metadata: `dev`, `ino`, `uid`, `gid`, `mode`, and `bytes` (32). Historical key hashes are not projected or claimed as current content verification. A future capture obtains current `mtimeNs` and `ctimeNs` by stat, retains those exact values, and requires stable metadata during capture and in before/after preservation. This component run used mocked metadata; it did not observe current key metadata or open, hash, or decode a real key.

The Programs-only tree walker admits `raw=None` solely for this authorized stat-only leaf, charges its 32 bytes and entry against the existing limits, and requires fixed-file and tree facts to agree. Inventory admission precedes tree content reads; a missing required key also stops capture before SQL. Legacy walkers and profiles remain unchanged. The production JavaScript closer is unchanged; its additional synthetic test proves lossless timestamp decoding without inventing a content hash or reading the named file.

The eight added Python methods cover authority/path substitutions, no-content stat access, missing/renamed keys, metadata drift, byte/entry/None limits and preservation. The added JavaScript guard covers the exact metadata shape. All filesystem operations involving the real key pathname are mocked; a small virtual bytes fixture exercises the 512 MiB boundary without that allocation.

The run used `/opt/goby-test/programs-stat-only-key-component-checks-20260916-ebf5c219b928`. Admission required 1 GiB available memory and 2 GiB root space; these are selected margins, not measured minima. The unit retained 256 MiB memory, swap disabled, CPU quota 25%, TasksMax 32, 60 seconds per group, 240 seconds per unit and a 300-second outer bound. Actual peak memory was 61,018,112 bytes. All 26 recorded PIDs and process groups were absent, the unit/cgroup and scratch were closed, and 52 saved streams were read back. Source/tool pins remained unchanged.

The original 138/142 evidence and consumed capture-01 remain separate and unchanged. Capture-01's failure record contains no filename: attribution to the generation master follows source order and later read-only directory metadata, not an original trace field. This correction neither rewrites that failure nor establishes a successful live capture. New source publication, a new reviewed capture input and execution remain pending; the transition hold stays in place.
