# Retained-account folder Guid verification

Status: completed and independently attested by the
[actual verification01 record](nextup-folder-grant-verification-01.md).
This document retains the recorded design; the consumed scope cannot resume.

Preparation03 proved that the complete Policy can echo numeric library grants
while the ordinary account's Views is empty. The separate selectable-folder
observation established the actual numeric Id, Guid, source-root Id and path
relations. The remaining question is whether the observed Guid pair supplies
restricted access under P's own token. This experiment changes only the
already owned P account's EnabledFolders and restores its complete old Policy.

## Frozen authority

Use the independently sealed preparation03 state, input and full cleanup public
snapshot; the independently attested folder-authority02 input/terminal and its
actual SelectableMediaFolders, VirtualFolders and Devices responses; and the
three recent exact-token login/logout/rejection chains. The historical full
snapshot has eight users, ten libraries and ninety devices. The later actual
Devices response has ninety-one rows, including its one closed observer device;
it is not a replacement complete public snapshot.

P is the retained ordinary account `2efabe1842ac4e34a1bcda05d6ae8de6`.
Its full original Policy has 45 fields and includes AuthenticationProviderId.
Do not reconstruct it from Q, a creation template or selected fields. The exact
canonical-JSON-plus-newline Policy SHA-256 is
`dba7e371d02fe2d219dd33141c8d8f103897635690f1b2f56b74e2fa9160a881`.
EnableAllFolders, IsAdministrator and IsDisabled remain false. The only
candidate change replaces EnabledFolders 101/103 with the two actual Guids:

- LA101: `b953914417ce42b5a5e07a3e8f6fd386`.
- LB103: `38fbd09cb8af4731993dd77e1750457f`.

All source/input descriptors, existing lock identity, original application and
existing proxy metadata, output root, old roots and finite request budgets must
be frozen before execution. Original implementation/data roots remain excluded.
No old output root is resumed or adopted as the new journal.

## Observation and compensation

Authenticate the administrator on a fresh device and obtain a complete actual
before snapshot. It contains the existing six detail witnesses plus six new
administrator-token, subject-P episode witnesses: 43 GETs for eight users and
ten libraries. These are new observations, not a historical own-token P baseline.
Require the fresh management-library DTOs to match both the retained complete
library snapshot and the actual selectable-folder observation's management
response, including their Guid and path relationships. Read the complete
original P Policy, then issue exactly one candidate Policy write and verify
its full readback.

Authenticate P with another fresh device. Require its own complete profile,
Views containing exactly LA101/LB103, both complete catalogs matching the frozen
semantic item map and all six complete episode details in zero playback state.
Each catalog legitimately contains seven rows, including the native root
102/104. Episode IDs are A1/A2/A3 = 109/108/110 and B1/B2/B3 = 114/115/116;
their order is determined by season/episode facts, not numeric ID order.
Check Id, ParentId, SeriesId, SeasonId, season/episode numbers, runtime and
complete UserData presence. Do not discard IsFavorite or missing/null distinctions.
After the positive own-token Views observation, record one additional P-token
SelectableMediaFolders GET for the future compatibility endpoint boundary.
Retain its bounded status/body without requiring HTTP 200 for Guid acceptance;
this extra read does not introduce another policy candidate or mutation.

Restore the exact original full Policy with a separate recorded write. Require
complete readback equality and the same P token's Views to return to the empty
original restriction. Collect the complete actual after snapshot and compare
all public documents, policies, configuration, preferences and projections.
Only the exact administrator/P authentication date fields and their two owned
devices may change within their actual login/response windows. Q remains wholly
unchanged. Finally log out each acknowledged token and prove same-token401.

No playback, PlayedItems cleanup, new account/library creation, media writes,
all-folders grant or original database access is allowed in this experiment.
The expected successful path is 59 normal plus 50 cleanup requests. The frozen
ceilings are 70 normal, 60 cleanup and 130 total, with ten seconds per request,
600 seconds per phase, 256 KiB per response, 64 MiB total response budget and a
32 MiB cleanup reserve. A systemd deadline bounds the complete worker lifetime.

Durably record each intent before dispatch and retain the actual raw response
before accepting ownership. Never retry an uncertain request. A lost or
unacknowledged policy write, failed ownership commit or incomplete journal
requires independent recovery in a separate scope. Known failures compensate
using the exact original Policy and close acknowledged sessions. The final
worker result cannot itself release a matrix fixture; independent root review,
process closure and external Goby/history preservation remain required.

## Follow-on work

A positive result establishes the reference grant contract only. It does not
complete NextUp preparation, four playback calibrations, global matrix behavior,
client refresh or the main upgrade. Fresh preparation needs a reviewed current
population/budget contract. Any Goby DTO or authorization changes must preserve
existing stable library IDs, native management behavior and stored grants.
