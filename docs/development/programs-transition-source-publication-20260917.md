# Programs transition sources published

The source publisher completed with original tool exit 0 (`1b8f88`) and
published the caller, closer and operator to the new scope ending
`programs-transition-dispatch-20260917-72c177d47421`. Their readback pins are
`f38233c4...`, `5f2664a2...` and `72c177d4...`; the [checkpoint](programs-transition-source-publication-20260917.json)
records the complete manifest and publication receipt.

Runtime `2bbf5e4c...`, input `d0006145...` and all five existing helpers retain
their original paths and were actually read back. The product composite/closure
and captured state/summary bindings were also reread. The earlier operator
`9b8d5e8a...` and all other existing files remain unchanged.

The PowerShell publisher ran; the published caller, closer and operator did not.
No transition, service operation, SQL, lock operation or test ran in this
publication scope. It supplies source publication/readback only, with no v4 or
client acceptance.

The M5 full scope `0add3118b99f` was active at publication under original SSH
session `43043`. It subsequently exited 1 (terminal chunk `91b1e8`); original
failure evidence and independent closure review remain pending. Fresh Programs
entry follows those reviews and the existing before-state/recovery-policy checks.
Normal transition checks the explicit recovery policy; actual recovery requires
an actual failure and separate input. M5 need not pass for that separate work.
This documentation update read the saved publication stdout and root's terminal report.
