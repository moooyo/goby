# Reference NextUp matrix 05 startup

Checkpoint: `2026-09-12T23:17:38Z`. The matrix05 operator stopped during admission
with exit2 before creating its persistent operator journal or matrix output.
The independently checked source control flow, failed-unit record, and absent
output roots establish zero business HTTP from this invocation. All 168
protected roots and the complete Goby state remain unchanged. This is a failed,
consumed startup; it is not a matrix result or client acceptance.

Preparation05 remains a successful four-calibration worker result. Its
[bounded terminal check](nextup-global-reference-preparation-05-worker-terminal.json)
binds 269 requests, four distinct stopped lifecycles and DELETE attempts, three
closed actor sessions, and unchanged preparation drafts. The separate
[assembly record](nextup-global-reference-matrix-05-assembly.json) joined all
269 actual request triples, 34 calibration response witnesses, and the three
exact token-closure pairs. It explicitly retains pending operator admission
and does not claim that the complete producer replay has been admitted.

## Startup and independent closure

Paths use `/opt/goby-test/exec-work-m3e` as `W`.

| Evidence | Value or SHA-256 |
| --- | --- |
| Operator source, `nextup-global-preparation-tool-09/revision-02` | `8c9ebe40bdc6224670b54298927a4ad0cff90450b3f05bd47963086fb0d3251e` |
| Attestation, `reference-nextup-global-matrix-attestation-05/attestation.json` | `e11d824a35b24b4c9901b32aec632c7e7f8088587406901d844b3a6c044ec5e3` |
| Assembly record | `824c039b0e687614bd1204162dc5bed710386ffab0d88d0df465b10075506b94` |
| Unit | `goby-nextup-global-reference-matrix-05.service` |
| Invocation | `48b19d7cd45045e7a856bd56ae5d7bde` |
| Former PID / exit | `1679323` / `2` |
| Finalized unit file | `a756138a22846bb8205204ee5957fec6cfbc4f2b8fc18b3aed049d63d0371b38` |
| Launch result | `b6ba5ae6121926d857a6f9a5090f74cbecea20f7e3981c40701029cb40940a04` |
| [Independent startup terminal](nextup-global-reference-matrix-05-startup-terminal.json) | `4f97d38366fbb480cc5389cf2682a5b76b94c9997621f51543001dbd9acec9be` |

The unit remains failed/MainPID0; its former process and cgroup are absent.
`W/nextup-global-reference-operator-runs-05` contains only the two unit logs.
The operator output `operator-05` and matrix output
`W/nextup-global-reference-runs-05/matrix-05` were never created. The matrix
parent remains empty. The zero-HTTP conclusion binds the exact frozen sources
and their journal-before-transport control flow; it is not a reference database
comparison or a claim about unrelated server activity.

Execution records are in `W/reference-nextup-global-matrix-execution-05`.
Before preservation SHA-256 is
`a4d01e46836960dacb33a38ff162e74c7eed21789bb121e60e52294785c206a3`;
after is `a93d68a96af00d61aff312973284ea6a7dfcb114e2126bdda4ffaef8547c4bbe`.
All 168 roots, old service identities, main files, and full Goby state at
83 sessions, 70 devices and 185 activity entries agree.

## Read-only diagnosis and repair boundary

The [separate diagnostic](nextup-global-reference-matrix-05-diagnostic.json),
SHA-256 `a428eeb3e936cb9ee8be4dc620e7323c0585e4137fcd06023bc898f3c9a2f3fb`,
reproduced an `OperatorError` in `_observer_shutdown`, before the diagnostic
runtime check. This is the diagnostic's stack, not an invented stack for the
original failed process. It did not call `execute` or send HTTP.

The accepted baseline observer's `ExecStart` retained the same complete path,
argv and `ignore_errors=no` prefix. Its execution annotations changed from the
recorded start/stop times, PID1515339, `code=exited`, `status=0` to the observed
`start_time=[n/a]`, `stop_time=[n/a]`, `pid=0`, `code=(null)`, `status=0/0` form
after the new unit's daemon reloads. Every other compared observer property,
including InvocationID, ExecMainPID and exit status, remained exact. The
completed preparation05 unit also matched its attestation exactly.

The repair preserves complete original-string equality and adds only this
observed reset representation when the recorded completed-command suffix
binds the verified former PID. Command arguments, source/input/plan hashes,
all other unit properties, empty cgroup and absent former PID remain mandatory.
The original observer evidence and raw verifier are unchanged. The two changed
operator files passed 80 remote guards and two compile checks, including seven
new groups for the reset representation and rejection boundaries. The actual
269-request/829-private-file replay also passed before its diagnostic runtime
was correctly rejected. See the [matrix06 record](nextup-global-reference-matrix-06.md).

Matrix06 subsequently passed actual operator admission, then stopped before
business HTTP at a distinct target-process metadata check. Both failed scopes
remain retained. Preparation05 is never rerun, and its still-absent matrix
output has not been consumed. The reference matrix, Goby comparison, positive
client gate, primary upgrade and full M2-M6 remain open; M7 remains deferred.
