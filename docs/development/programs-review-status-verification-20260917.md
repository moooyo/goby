# Programs review-status verification, 2026-09-17

The Python product loader and JavaScript closer now require `passed` for the retained complete-source independent review and retain `verified` for the legacy profile. The source archive selects one status; neither profile accepts both. The original review fields, eleven checks, limits, artifact pins, runtime identities and transition hold remain unchanged. Python's existing review schema check is now a pure function called at the same loader position. The tests cover both accepted statuses and rejection of the other profile's status.

One run on `ssh test-env` passed 152 component checks: 45 runtime, 36 successor and 71 closer, with zero failures, skips or retries. The scope was `/opt/goby-test/programs-review-status-component-checks-20260917-f33f2213eaed`; the twelve-file snapshot contains the four changed source/test files and eight unchanged files copied from the closed 151-check scope. Exact pins are in the [JSON checkpoint](programs-review-status-verification-20260917.json).

The original SSH session `80590` exited with code 0. The runner took 15.312 seconds, the dispatcher took 16.513 seconds, and unit peak memory was 67,907,584 bytes. The unchanged bounds were 256 MiB memory, swap disabled, CPU quota 25%, TasksMax 32, 60 seconds per group, 240 seconds per unit and a 300-second outer timeout. Admission observed 5,128,228,864 bytes available memory and 45,092,372,480 bytes root space against the selected 1 GiB / 2 GiB margins. Source/tool pins remained unchanged; all recorded PIDs and process groups were absent, the cgroup was absent, scratch was empty, and 56 raw streams totaling 43,090 bytes were read back. Readback descriptors were closed and checked. The caller acquired no OS or file lock; process exit supports closure of owned process descriptors, without a separate process FD census.

The parent agent independently read the complete code diff and actual result, the two Python stderr tails and the closer stdout; it compared remote hashes for result, unit closure, session closure and those three streams, and matched all four local changed files to the tested source hashes. It accepted the component result without replaying tests or adding a live check to that review. This is a saved-evidence review method, not a new independent-review artifact.

No application SQL, HTTP, browser run, application service action, real key-content read, capture, transition or actual product-loader invocation ran in this component scope. The original 138/142/151 records and failed product readback remain unchanged. A new actual product-loader readback is still required; these component results do not establish product acceptance or an actual runtime-v4 outcome.
