# Programs transition caller-fault components, 2026-09-17

The saved `caller-faults` component run passed **8 mocked methods once, with zero failures or skips**. There were no automatic reruns or reused scores. The original SSH command returned exit code 0 synchronously in tool chunk `c142a0`; no pending execution session was created. Exact source and retained remote evidence pins are in the [JSON record](programs-transition-caller-fault-components-20260917.json).

The scope was `/opt/goby-test/programs-transition-caller-fault-component-checks-20260917-7b40073ae8a3`. Coverage includes owned cleanup despite registration or receipt failures, preservation of an exited leader's result, terminal-evidence ordering, listener-inode drift rejection, original-exit evidence binding, and separate CLI, caller and SSH failure classification.

The saved closure records report complete owned cleanup: recorded PIDs and process groups absent, component and original SSH-session cgroups empty, scratch empty, unchanged source and retained tool pins, and no cleanup errors. No lock was acquired. This does not claim an independent process FD census.

All subject-function filesystem, descriptor, process, command, clock and service effects used finite mocks. The final garbage-collection process census and the closer's `inspect_attempt` after its finite evidence boundary were mocked; their real behavior is not covered. The methods establish ordering and classification within those boundaries, not a complete live census or saved-receipt graph.

The PowerShell source did not execute, and no standalone actual caller or closer command ran. The earlier runtime 45, successor 46 and closer 71 groups were not rerun or added to this count. Only the owned component infrastructure unit was created and stopped. No SQL, HTTP, browser, application or PostgreSQL service action, actual capture, Programs transition, recovery or standalone product-loader invocation occurred.

This record supports only the eight mocked methods and their owned execution closure. It does not authorize an actual Programs transition or establish complete live caller/closer behavior. This documentation update reran no verification.
